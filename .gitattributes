# Keep repository text files consistent across Windows, macOS, and Linux.
* text=auto eol=lf

# Windows command scripts require CRLF for compatibility with older tooling.
*.bat text eol=crlf
*.cmd text eol=crlf
